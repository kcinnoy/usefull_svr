let express,cors,readdirSync,mongoose;_4ce‍.w("express",[["default",["express"],function(v){express=v}]]);_4ce‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_4ce‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_4ce‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require("morgan");
require("dotenv").config();

// create express app
const app = express();

//db
mongoose.connect(process.env.DATABASE, {
    userNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
    useCreateIndex: true,
}).then();


//apply middleware
app.use(cors());
app.use(express.json());
app.use(morgan("dev"));
app.use((req,res, next) => {
    console.log("custom middleware placeholder ");
    next();
});

//route 
readdirSync('./routes').map((r) => app.use('/api', require(`./routes/${r}`)))

const port = process.env.PORT || 8000;

app.listen(port,() => console.log(`server is running on port ${port}`));