let bcrypt,compare;_cfe‍.x([["hashPassword",()=>hashPassword],["comparePassword",()=>comparePassword]]);_cfe‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}],["compare",["compare"],function(v){compare=v}]]);

       const hashPassword = (password) => {
    return new Promise((resolve, reject) => {
        bcrypt.genSalt(12,(err, salt) => {
            if(err) {
                reject(err);
            }
            bcrypt.hash(password, salt, (err,hash) => {
                if (err) {
                    reject(err);
                }
                resolve(hash);
            });
        });
    });
};

       const comparePassword = (password, hashed) => {
    return bcrypt,compare(password, hashed);
};